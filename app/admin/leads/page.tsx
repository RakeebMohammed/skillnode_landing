"use client";
import { useEffect,useState } from 'react';
export default function LeadsPage(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/leads',{cache:'no-store'}).then(r=>r.json()).then(x=>setRows(x.leads||[]))},[]);function exportCsv(){const headers=['Name','Email','Phone','Source','City','Country','IP','Created'];const lines=[headers,...rows.map(r=>[r.name,r.email,r.phone||'',r.source||'',r.city||'',r.country||'',r.ip||'',new Date(r.createdAt).toISOString()])].map(a=>a.map(v=>`"${String(v).replaceAll('"','""')}"`).join(','));const blob=new Blob([lines.join('\n')],{type:'text/csv'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='skillnode-leads.csv';a.click();URL.revokeObjectURL(a.href)}return <div><div className="admin-top"><div><div className="crumb">Home / Analytics / Leads</div><h1>Leads</h1><p>Every submission from your landing page.</p></div><button className="primary-btn" onClick={exportCsv}>Export CSV</button></div><div className="panel table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Source</th><th>Location</th><th>Device</th><th>Created</th></tr></thead><tbody>{rows.map(r=><tr key={r._id}><td><b>{r.name}</b></td><td>{r.email}</td><td>{r.phone||'—'}</td><td>{r.source||'Direct'}</td><td>{r.city||'—'}, {r.country||'—'}</td><td>{r.device||'—'}</td><td>{new Date(r.createdAt).toLocaleString()}</td></tr>)}</tbody></table></div></div>}
