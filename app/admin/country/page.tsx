"use client";
import { useEffect, useMemo, useState } from "react";
export default function CountryPage() { const [rows, setRows] = useState<any[]>([]); useEffect(() => { fetch("/api/admin/insights?kind=countries").then(r => r.json()).then(x => setRows(x.rows || [])); }, []); const total = useMemo(() => rows.reduce((sum, row) => sum + row.visitors, 0), [rows]); return <main><div className="admin-top"><div><div className="crumb">Analytics workspace / Country</div><h1>Visitors by country</h1><p>Location, IP addresses and engagement by country.</p></div></div><div className="stat-grid"><div className="stat-card"><span>Countries</span><strong>{rows.length}</strong><small>Recorded locations</small></div><div className="stat-card"><span>Visitors</span><strong>{total}</strong><small>Across all countries</small></div><div className="stat-card"><span>Top country</span><strong>{rows[0]?.country || "—"}</strong><small>By unique visitors</small></div></div><div className="panel table-wrap"><table><thead><tr><th>Country</th><th>Visitors</th><th>Sessions</th><th>Views</th><th>IP addresses</th><th>Last seen</th></tr></thead><tbody>{rows.map(row => <tr key={row.country || "unknown"}><td><b>{row.country || "Unknown"}</b></td><td>{row.visitors}</td><td>{row.sessions}</td><td>{row.views}</td><td className="mono truncate-cell" title={(row.ips || []).join(", ")}>{(row.ips || []).filter(Boolean).join(", ") || "Unavailable"}</td><td>{new Date(row.last).toLocaleString()}</td></tr>)}</tbody></table>{!rows.length && <div className="empty">Country data is populated when a public IP address is available.</div>}</div></main>; }
