"use client";
import { useEffect, useState } from "react";
export default function ClicksPage() { const [rows, setRows] = useState<any[]>([]); useEffect(() => { fetch("/api/admin/insights?kind=clicks").then(r => r.json()).then(x => setRows(x.rows || [])); }, []); return <main><div className="admin-top"><div><div className="crumb">Analytics workspace / Link Clicks</div><h1>Link & CTA clicks</h1><p>See which calls to action people choose.</p></div></div><div className="panel table-wrap"><table><thead><tr><th>Label</th><th>Destination</th><th>Element</th><th>Clicks</th><th>Visitors</th><th>Last click</th></tr></thead><tbody>{rows.map((row, index) => <tr key={`${row.href}-${index}`}><td><b>{row.label || "Unlabelled element"}</b></td><td className="truncate-cell" title={row.href}>{row.href || "—"}</td><td>{row.element || "—"}</td><td>{row.clicks}</td><td>{row.visitors}</td><td>{new Date(row.last).toLocaleString()}</td></tr>)}</tbody></table>{!rows.length && <div className="empty">Clicks will appear after visitors interact with a link or button.</div>}</div></main>; }
