"use client";
import { useEffect, useState } from "react";
export default function OrganicAiPage() { const [rows, setRows] = useState<any[]>([]); useEffect(() => { fetch("/api/admin/insights?kind=organic-ai").then(r => r.json()).then(x => setRows(x.rows || [])); }, []); return <main><div className="admin-top"><div><div className="crumb">Analytics workspace / Organic Search</div><h1>Organic search traffic</h1><p>Search engines that bring visitors to your site.</p></div></div><div className="panel table-wrap"><table><thead><tr><th>Source</th><th>Channel</th><th>Sessions</th><th>Visitors</th><th>Page views</th></tr></thead><tbody>{rows.map((row, index) => <tr key={`${row.source}-${index}`}><td><b>{row.source || "Direct"}</b></td><td>{row.channel || "Organic Search"}</td><td>{row.sessions}</td><td>{row.visitors}</td><td>{row.views}</td></tr>)}</tbody></table>{!rows.length && <div className="empty">Organic search data will appear when visitors arrive from a recognized search engine.</div>}</div></main>; }
