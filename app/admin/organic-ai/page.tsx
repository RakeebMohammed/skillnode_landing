"use client";
import { useEffect, useState } from "react";
export default function OrganicAiPage() { const [rows, setRows] = useState<any[]>([]); useEffect(() => { fetch("/api/admin/insights?kind=organic-ai").then(r => r.json()).then(x => setRows(x.rows || [])); }, []); return <main><div className="admin-top"><div><div className="crumb">Analytics workspace / Organic & AI Traffic</div><h1>Organic & AI traffic</h1><p>Search, referrals and AI assistants that bring visitors to your site.</p></div></div><div className="panel table-wrap"><table><thead><tr><th>Source</th><th>Channel</th><th>Sessions</th><th>Visitors</th><th>Page views</th></tr></thead><tbody>{rows.map((row, index) => <tr key={`${row.source}-${index}`}><td><b>{row.source || "Unknown"}</b></td><td>{row.channel || "Unclassified"}</td><td>{row.sessions}</td><td>{row.visitors}</td><td>{row.views}</td></tr>)}</tbody></table>{!rows.length && <div className="empty">Organic and AI referral traffic will appear here when referrer data is available.</div>}</div></main>; }
