"use client";
import { useEffect, useState } from "react";
export default function PagesPage() { const [rows, setRows] = useState<any[]>([]); useEffect(() => { fetch("/api/admin/insights?kind=pages").then(r => r.json()).then(x => setRows(x.rows || [])); }, []); return <main><div className="admin-top"><div><div className="crumb">Analytics workspace / Page & Post Views</div><h1>Page & post views</h1><p>Find the pages that hold your audience’s attention.</p></div></div><div className="panel table-wrap"><table><thead><tr><th>Page</th><th>Views</th><th>Unique visitors</th><th>Last viewed</th></tr></thead><tbody>{rows.map(row => <tr key={row.page}><td><b>{row.page || "/"}</b></td><td>{row.views}</td><td>{row.visitors}</td><td>{new Date(row.last).toLocaleString()}</td></tr>)}</tbody></table>{!rows.length && <div className="empty">Page-view data will appear as visitors browse your site.</div>}</div></main>; }
